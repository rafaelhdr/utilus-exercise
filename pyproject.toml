[project]
name = "utilus-exercise"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0",
    "freezegun>=1.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
